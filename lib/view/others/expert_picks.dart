import 'package:flutter/material.dart';

class ExpertPicks extends StatelessWidget {
  const ExpertPicks({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Expert Picks'),
      ),
      body: const Center(
        child: Text('Coming soon !'),
      ),
    );
  }
}
